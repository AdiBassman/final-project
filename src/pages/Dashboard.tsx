import PageStub from '../components/PageStub'

// Role-aware: tutors see requests received, students see requests sent (Phase 6).
export default function Dashboard() {
  return (
    <PageStub
      title="Dashboard"
      note="Role-aware: tutors see lesson requests received; students see requests they sent (Phase 6)."
    />
  )
}
